<div class="min-h-screen bg-[#F1EFE9] flex items-start justify-center px-4 pt-24 pb-12">
  <div class="w-full max-w-md">

    <%# --- 有効期限切れエラーメッセージ --- %>
    <% if params[:expired] %>
      <div class="mb-6 p-6 bg-white border-l-4 border-[#A75F29] shadow-md rounded-r-lg animate-in fade-in slide-in-from-top-4 duration-500">
        <div class="flex items-center gap-2 mb-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-[#A75F29]" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
          </svg>
          <p class="text-[#A75F29] font-bold text-sm tracking-wider">リンクの有効期限が切れています</p>
        </div>

        <p class="text-xs text-[#8C7B6C] font-bold leading-relaxed">
          セキュリティ保護のため、再設定用のリンクは一定時間で無効となります。<br>
          お手数ですが、再度メールアドレスを入力して申請を行ってください。
        </p>
      </div>
    <% end %>

    <%# --- メインカード --- %>
    <%# ↓ ここにあった重複したレイアウト用divを削除しました %>
    <div class="w-full max-w-md bg-white border-2 border-[#B5A89A] rounded-lg shadow-xl p-8 md:p-10">

      <%# --- ヘッダー --- %>
      <div class="text-center mb-8">
        <h2 class="text-2xl font-serif font-bold text-[#365442] tracking-widest">
          FORGOT PASSWORD
        </h2>
        <p class="text-[10px] text-[#8C7B6C] font-bold mt-1 uppercase tracking-[0.2em]">
          パスワードの再設定
        </p>
        <div class="mt-4 w-10 h-[1px] bg-[#D6CEC5] mx-auto"></div>

        <%# --- 案内テキスト --- %>
        <div class="mt-6 px-2">
          <p class="text-[13px] text-[#8C7B6C] leading-relaxed font-bold">
            登録済みのメールアドレスを入力してください。<br>
            再設定用のリンクをお送りします。
          </p>
        </div>
      </div>

      <%# --- フォーム --- %>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name)) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="mb-8">
          <%= f.label :email, "メールアドレス", class: "block text-xs font-bold text-[#365442] tracking-widest mb-2 uppercase" %>
          <%= f.email_field :email, 
                value: params[:email], 
                autofocus: true, 
                autocomplete: "email",
                placeholder: "example@mail.com",
                class: "w-full p-3 bg-[#FDFBF9] border-2 border-[#D6CEC5] rounded
                       focus:ring-2 focus:ring-[#365442] focus:border-[#365442] outline-none transition
                       font-normal text-lg text-[#365442] tracking-normal" %>
        </div>

        <div>
          <%= f.submit "メールを送信", 
                class: "w-full bg-[#365442] hover:bg-[#2A4034] text-[#F9F7F2] font-bold
                       py-4 rounded shadow-lg transition tracking-[0.2em] cursor-pointer" %>
        </div>
      <% end %>

      <%# --- フッターリンク --- %>
      <div class="mt-10 pt-8 border-t border-[#D6CEC5]/40 text-center">
        <%= link_to new_session_path(resource_name), 
              class: "text-[11px] font-bold text-[#8C7B6C] hover:text-[#365442] tracking-[0.15em] transition-colors border-b border-[#D6CEC5] pb-0.5 inline-flex items-center gap-1" do %>
          <span class="leading-none">ログインに戻る</span>
        <% end %>
      </div>

    </div> <%# メインカード終了 %>
  </div> <%# max-w-md終了 %>
</div> <%# 全体背景終了 %>